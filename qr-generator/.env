# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> symfony/lock ###
# Choose one of the stores below
# postgresql+advisory://db_user:db_password@localhost/db_name
LOCK_DSN=flock
###< symfony/lock ###

###> app ###
# Public identity of the site. Change these before going live.
SITE_NAME="QRLab"
SITE_URL="https://example.com"
CONTACT_EMAIL="contact@example.com"

# Monetisation: put your AdSense publisher id (ca-pub-XXXXXXXXXXXXXXXX) here.
# Ads are only ever injected when ADSENSE_ENABLED=1 *and* the visitor accepted them.
ADSENSE_ENABLED=0
ADSENSE_CLIENT=""
# Optional manual ad units. Leave empty to rely on AdSense Auto ads.
ADSENSE_SLOT_TOP=""
ADSENSE_SLOT_INLINE=""
ADSENSE_SLOT_FOOTER=""

# Lead capture: off | optional | download
#   off      – no email is ever requested
#   optional – a discreet opt-in form next to the download buttons
#   download – the visitor is asked for an email the first time they download
LEAD_CAPTURE_MODE=download

# Public gallery: every http(s) link turned into a QR code is listed on the home
# page and on the gallery page. Wi-Fi passwords, vCards, texts and phone numbers
# are never listed. Set to 0 to keep every generation private.
PUBLIC_GALLERY=1
# Hosts kept out of the gallery, comma separated (a host also covers its subdomains).
BLOCKED_HOSTS=""

# Optional privacy-friendly analytics (Google Analytics 4 id, e.g. G-XXXXXXX). Empty = disabled.
ANALYTICS_ID=""

# Security / proxy
TRUSTED_HOSTS=""
TRUSTED_PROXIES="127.0.0.1,REMOTE_ADDR"
###< app ###

###> doctrine/doctrine-bundle ###
# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
# IMPORTANT: You MUST configure your server version, either here or in config/packages/doctrine.yaml
#
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=8.0.32&charset=utf8mb4"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
# SQLite by default: zero infrastructure. Point it at PostgreSQL in production
# (and attach a volume if you keep SQLite) so the leads survive a redeploy.
DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
###< doctrine/doctrine-bundle ###
