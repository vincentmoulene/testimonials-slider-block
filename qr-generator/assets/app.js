import './styles/app.css';

// Boots Stimulus and registers everything in assets/controllers: the live
// preview, the email gate, the language menu and the consent banner. Without
// this import the page still works, but none of them run.
import './stimulus_bootstrap.js';
